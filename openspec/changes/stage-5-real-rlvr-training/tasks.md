## 1. 默认参数与输出目录

- [x] 1.1 更新训练脚本默认参数，优先采用当前机器上吞吐更高的 batch size、completion 长度和日志频率。
- [x] 1.2 将阶段 5 默认输出目录与阶段 4 的最小闭环输出区分开。
- [x] 1.3 更新训练配置摘要字段，确保默认参数变化在输出中可见。

## 2. 中等规模真实训练

- [x] 2.1 选定阶段 5 的中等规模训练命令，显著高于阶段 4 的极小训练规模。
- [x] 2.2 使用本地缓存模型路径运行一轮中等规模真实训练。
- [x] 2.3 确认训练完成后输出目录中保存 LoRA adapter 和配置摘要。

## 3. 耗时与吞吐记录

- [x] 3.1 记录中等规模训练的 `train_runtime`、`train_steps_per_second` 和 `train_samples_per_second`。
- [x] 3.2 汇总训练日志中的 reward 相关字段，确认阶段 5 不再只是联调闭环。
- [x] 3.3 基于实测吞吐给出更大规模训练的大致时间估算。

## 4. 文档更新

- [x] 4.1 更新 `README.md`，将阶段 5 改为真实 RLVR 训练，并写入推荐默认参数。
- [x] 4.2 更新 `plan.md` 和 `AGENTS.md`，把阶段 5 定义为中等规模真实训练而非独立评估。
- [x] 4.3 在文档中明确说明阶段 5 的必达目标不是 GSM8K 全集训练。

## 5. 验证

- [x] 5.1 运行 Python 编译检查，确认训练参数调整后脚本语法正确。
- [x] 5.2 运行阶段 2、阶段 3 检查和阶段 4 dry-run，确认默认参数更新未破坏现有流程。
- [x] 5.3 运行阶段 5 中等规模真实训练并记录实际耗时。
- [x] 5.4 运行 `openspec validate stage-5-real-rlvr-training --strict`。
