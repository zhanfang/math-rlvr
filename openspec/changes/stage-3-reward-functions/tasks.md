## 1. Reward 模块

- [x] 1.1 新增阶段 3 reward 模块，放置模型答案抽取、数值等价和 reward 函数。
- [x] 1.2 实现 `<answer>...</answer>` 模型答案抽取函数，缺少完整标签时返回空字符串。
- [x] 1.3 实现输出格式检查函数，要求 `<reasoning>` 和 `<answer>` 都存在且非空。
- [x] 1.4 实现基础答案清理，处理首尾空白和数字逗号。
- [x] 1.5 实现轻量数值等价判断，覆盖整数、小数、分数和带逗号数字。
- [x] 1.6 在数值解析失败时回退到清理后的字符串相等比较。

## 2. Reward 函数

- [x] 2.1 实现正确性 reward，答案等价时返回 `1.0`，否则返回 `0.0`。
- [x] 2.2 实现格式 reward，完整格式返回 `1.0`，否则返回 `0.0`。
- [x] 2.3 确认正确性 reward 可复用阶段 2 的 GSM8K 标准答案抽取结果。
- [x] 2.4 保持 reward 函数不下载模型、不运行模型生成、不触发训练。

## 3. 离线验证脚本

- [x] 3.1 新增阶段 3 reward 离线检查脚本。
- [x] 3.2 用硬编码样例覆盖正确答案、错误答案、分数与小数等价、逗号数字等价。
- [x] 3.3 用硬编码样例覆盖缺失 `<answer>`、缺失 `<reasoning>` 和空标签格式。
- [x] 3.4 运行脚本时打印每个样例的抽取答案、正确性 reward 和格式 reward。
- [x] 3.5 确认离线检查不依赖网络、数据集下载或模型权重下载。

## 4. 文档更新

- [x] 4.1 更新 `README.md`，加入阶段 3 reward 检查命令和预期输出。
- [x] 4.2 更新 `plan.md`，将当前阶段推进到奖励函数，并保留阶段 4 训练作为后续目标。
- [x] 4.3 更新 `AGENTS.md`，说明阶段 3 仍使用 `.venv/bin/python`，且不下载模型或训练。

## 5. 验证

- [x] 5.1 运行 Python 编译检查，确认新增模块和脚本语法正确。
- [x] 5.2 运行阶段 2 答案抽取离线检查，确认已有数据逻辑未回退。
- [x] 5.3 运行阶段 3 reward 离线检查。
- [x] 5.4 运行 `openspec validate stage-3-reward-functions --strict`。
