# Stage 7 RunPod CUDA/vLLM environment.
#
# Keep this file pinned. The training pod should not run open-ended
# `pip install -U ...` commands because dependency resolution can waste GPU
# rental time and can break an otherwise valid image.
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
triton==3.6.0
transformers==4.57.6
huggingface-hub==0.36.2
datasets==4.8.5
accelerate==1.13.0
peft==0.19.1
trl[vllm]==1.5.0
vllm==0.18.0
hf_transfer==0.1.9
